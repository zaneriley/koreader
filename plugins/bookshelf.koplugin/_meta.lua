local _ = require("gettext")

return {
    fullname = _("Library"),
    description = _([[Optional prototype library home for current reading, recently added books, all books, Dictionary, Add Books, and Files.]]),
}
